<%= link_to productos_path(category_id: category.id), 
    class: "category bg-green-300 hover:bg-green-200 flex items-center  text-black text-gray-600 px-4 py-2 rounded-xl drop-shadow-sm w-auto" do %>
  <%= category.name %>&nbsp;
<% end %>
